# RISK_REGISTER.md

## RC Cleanup Update (2026-03-10)
- Full backend test gate is now green: `npm test -- --runInBand` passed (21 suites / 49 tests).
- Integration and webhook hardening regression risks were reduced by fixing Jest ESM/module-cache instability and refreshing auth/payment hardening coverage.
- Email normalization inconsistency risk is mitigated in code (`register`, `login`, repository lookup, and login rate-limit keying all use normalized email).

| Risk Title | Category | Severity | Affected Files/Modules | Why It Matters | Mitigation Plan | Status | Verification Notes | Remaining Gaps |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Mongo RS not ready (transactions fail) | Data integrity / deployment | Critical | `docker-compose.yml`, `server/src/infra/mongo.ts`, `server/Dockerfile` | Order creation uses transactions; without PRIMARY app errors at runtime. | Auto-init RS, compose healthcheck waits for PRIMARY, app retries/pings before start. | Mitigated for RC | Integration suites (`auth->cart->order->payment`) pass against in-memory replica set; startup retry logic in place. | Validate containerized PRIMARY readiness in release environment. |
| Error responses inconsistent / no requestId in body | Observability / reliability | Low | `src/middlewares/errorHandler.ts`, `src/utils/apiResponse.ts`, `src/middlewares/notFound.ts`, `src/middlewares/auth.ts` | Hard to debug prod issues; clients get varied shapes; request tracing lost. | Standardize error shape, include requestId; update docs/tests. | Mitigated | Unit tests updated; Swagger includes error schema; manual curl should show requestId. | Keep new routes on the same response contract. |
| JWT misuse or refresh reuse | Auth/security | Medium | `src/services/authService.ts`, `src/utils/jwt.ts`, `src/utils/tokenBlacklist.ts` | Token replay gives unauthorized access. | jose with iss/aud/nbf, JTI blacklist, refresh rotation/reuse detection; add broader integration tests. | Mitigated | Auth hardening tests now cover refresh-reuse and logout invalidation/blacklist behavior. | Continue monitoring auth-state dependency on Redis availability. |
| Weak/breached passwords | Auth/security | Medium | `src/services/authService.ts`, `src/validators/auth.ts`, `src/utils/passwordBreach.ts` | Compromised credentials increase breach risk. | Enforce zxcvbn score; optional HIBP k-anonymity check; fail weak passwords. | Mitigated in code | Unit tests cover weak password, breached password, and strict HIBP outage. | Runtime HIBP behavior still needs live validation if enabled. |
| Payment webhook spoof/replay | Payments/security | Medium | `src/controllers/paymentController.ts`, `src/services/paymentService.ts`, `src/services/paymentProviders/PayWayProvider.ts`, `models/PaymentWebhookLog.ts` | Fake/replayed webhooks could mark orders paid. | Signature/secret validation, idempotency keys, logging, tests with mocked providers. | Mitigated | Tests cover Stripe valid/invalid signatures + duplicate events, and PayWay signature/idempotency handling. | Keep provider contract tests current on SDK/API upgrades. |
| Missing CI/lint/build gate | Testing/deployment | High | `.github/workflows/ci.yml`, `package.json`, `.eslintrc.cjs` | Regressions/security issues can ship. | Add CI pipeline (lint/test/build/docker). | Mitigated | Local gate now green (`lint`, full `test`, `build`), workflow file present. | Keep CI green in target branch before release tag. |
| Container runs as root / large image | Deployment/security | Medium | `server/Dockerfile`, `docker-compose.yml` | Larger attack surface; slower deploy. | Multi-stage, `npm ci --omit=dev`, non-root, healthcheck. | Open | `docker build`; container user != root. | None. |
| Metrics/observability missing | Observability | Medium | `src/app.ts`, `src/config.ts`, logging | Hard to detect prod issues. | Add `/metrics`, configurable log level, doc rotation/transport. | Open | `curl /metrics`; env toggles log level. | No rotation yet. |
| Data validation gaps (phone, slug) | Data integrity | Medium | `validators/order.ts`, `validators/catalog.ts`, `services/catalogService.ts` | Bad data, duplicates in prod. | Strengthen validators; enforce slug uniqueness in service. | Open | Validator tests. | DeviceId middleware reuse pending. |
| Payments/log growth | Ops/performance | Low | `PaymentWebhookLog` | Unbounded log growth. | Add retention/TTL or archiving. | Open | Check TTL/index. | TTL not set. |
| Dependency lockfile drift | Reliability | Medium | `server/package-lock.json` (not updated after new deps) | Reproducible installs/builds may fail; CI could diverge. | Regenerate lockfile with `npm ci`/`npm install`; commit updated lock. | Open | Successful clean install with updated lock. | None once lock committed. |
